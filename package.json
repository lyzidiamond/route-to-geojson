{
  "name": "route-to-geojson",
  "version": "0.0.1",
  "description": "Converts a Mapbox Directions API v5 route response to a GeoJSON file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "route-to-geojson": "./cli.js"
  },
  "author": "Lyzi Diamond",
  "license": "ISC",
  "dependencies": {
    "@mapbox/polyline": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.9.1"
  }
}
